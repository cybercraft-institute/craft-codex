{
  "name": "craft-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Mixed-Reality knowledge base for the trades. Open-source MIT — voice + hologram + master-surface, starting with the dovetail joint.",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "predev": "pnpm --filter @craft-codex/core build",
    "dev": "pnpm --filter @craft-codex/tischler dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm --filter @craft-codex/tischler lint",
    "boundary-check": "pnpm --filter @craft-codex/core boundary-check"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
